# Copy this file to .env and fill in real values. Never commit .env.

# LinkedIn session cookie, obtained by logging into linkedin.com in a real
# browser and copying it from DevTools > Application > Cookies. See
# README.md "Getting your LinkedIn session cookie".
LI_AT_COOKIE=

# Clients of this API must send this value in the X-API-Key header.
# Generate one yourself, e.g.: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=

# Optional tuning (defaults shown)
MIN_REQUEST_INTERVAL_SECONDS=3.0
CACHE_TTL_SECONDS=3600
CACHE_MAX_SIZE=512

# On a CPU-starved free-tier host, keep this a few seconds BELOW your
# platform's own request/proxy timeout (e.g. Render's free tier cuts off
# around ~30s) so this app returns a clean JSON error instead of the
# platform silently returning an empty 502. See README Known Limitations.
REQUEST_TIMEOUT_SECONDS=20.0
